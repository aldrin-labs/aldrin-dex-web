import React from 'react'

import { PageTemplate, Header, Footer } from '../../../components'

const HomePage = () => (
  <PageTemplate
    header={<Header />}
    hero={<div>LOL HI</div>}
    footer={<Footer />}
  >
    <div>LOL</div>
  </PageTemplate>
)

export default HomePage

